产品需求文档：P1 核心引擎 MVP (Online C++ Editor)版本: v0.9 (初稿)作者: Edward Ma (根据 AI 项目经理指导)日期: 2025年10月31日项目阶段: P1 (核心引擎)目标: 验证 C++ 代码在线执行和结果反馈的核心技术可行性。1. 项目愿景与目标1.1 产品成功目标核心功能验证: 证明可以安全、稳定地在 Web 界面上编译和运行用户提交的 C++ 代码，并展示结果。技术准备: 建立未来 P2/P3 扩展所需的健壮的前端和后端架构骨架。1.2 团队训练与成长目标Edward Ma: 掌握 React/Node.js 的基础全栈开发流程，并学习 Git/PR 的工程管理实践。Jim Ma: 成功参与至少 1-2 个功能的实现，熟悉代码规范和 PR 协作流程。Edward Ma (开发负责人): 成功主导和管理首次迭代，推动 PR 流程的落地。2. 核心功能 (Feature Set)P1 MVP 专注于实现类似 CPP.sh 的核心功能。2.1 C++ 编辑器 (Frontend: Editor)ID功能描述验收标准 (Acceptance Criteria)P1.1代码输入界面必须集成 Monaco Editor，提供专业级的 C++ 语法高亮和行号。P1.2深色主题编辑器 UI 必须使用深色主题，以模拟专业 IDE 环境。P1.3运行按钮必须有清晰的 "运行 (Run)" 按钮，触发后端执行流程。2.2 代码执行引擎 (Backend: Execution)ID功能描述验收标准 (Acceptance Criteria)P1.4API 客户端集成Node.js 后端必须成功调用 Piston 或 Judge0 API，并接收执行结果。P1.5/api/run 接口必须有一个安全的 API 接口，接收前端的 C++ 代码字符串。P1.6代码封装后端在发送代码给第三方 API 前，需要确保 C++ 代码被正确封装以便运行 (如包含 main() 函数和必要的头文件)。2.3 结果展示 (Frontend: Console)ID功能描述验收标准 (Acceptance Criteria)P1.7标准输出 (stdout)必须在前端控制台区域，清晰展示 C++ 代码的正常运行输出。P1.8错误信息 (stderr/Compile Error)如果代码编译失败或运行时出错，必须清晰展示编译器或运行时的错误信息。P1.9加载状态在代码执行期间 (等待 API 响应)，必须显示 "正在编译..." 或加载动画。3. 技术要求与架构 (Architectural Decisions)领域要求或决策备注FrontendReact + Tailwind CSS使用 monaco-editor 库实现 P1.1。BackendNode.js (Express/Fastify)负责路由和 API 调用。Code ExecutionPiston API 或 Judge0 API必须使用第三方 API 以保证安全沙箱环境。版本管理GitHub & Git Flow (简化 TBD)Branching Policy: main (Production) $\leftarrow$ develop (Staging) $\leftarrow$ feature/xxxCI/CD待定 (在 P1 稳定后配置 GitHub Actions)初步只要求本地运行。4. 验收标准总结 (MVP Done Criteria)所有 P1.1 至 P1.9 的核心功能均已实现。应用可以被部署到测试环境，并成功运行一个基本的 C++ "Hello World" 示例。Edward 和 Jim 已成功完成一次 Code Review 和 PR 合并 的流程。NOVICE_JOURNAL.md 和 ARCHITECTURE.md 文件已创建并包含 P1 阶段的初期决策。
