好的，这是一个很好的更新！这个目标更清晰，更符合“先跑起来”的教学思路。

我将用这个新目标更新第三部分，并增加一个“自己改代码”的步骤，以匹配 15 分钟的目标。

-----

# 🤖 AI 老师换人了？三句话教会新 AI 助教接手你的编程项目

> **项目:** 网页 C++ IDE (`cpp-dojo`)
> **目标读者:** 零项目经验的中学生（只要会复制粘贴）
> **目标:** 演示如何从一个 AI 切换到另一个 AI，并让新 AI 成功接手项目。

-----

## 📖 第一部分：为什么 AI 老师会“失忆”？

你是否遇到过这种情况？

昨天，你和“AI 老师 A”聊得热火朝天，他带着你搭建了 `cpp-dojo` 项目的前端界面。今天，你打开一个新的聊天窗口，“AI 老师 B”却一脸迷茫地问你：“你好，有什么可以帮你的吗？”

他完全“忘记”了你的项目！

### 👨‍🏫 人类老师 vs AI 老师

  * **人类老师：** 如果换了个新老师，他会去和前一个老师交接，或者查看你的“学生档案”。
  * **AI 老师：** AI 没有“长期记忆”。每个聊天窗口都是一个**全新的教室**。新 AI 老师不知道你上节课学了什么。

### ❌ 错误的“交接”方式：上下文倾倒（Context Dumping）

你可能会很着急，把整个项目文件夹、所有代码、几十个文件一次性丢给 AI 老师 B。

> **这就是“上下文倾倒”**。
>
> 就像你把整个书包——包括体育课的脏衣服、吃剩的零食和揉成一团的草稿纸——都倒在老师桌上，然后说：“老师，我的作业在里面，你自己找吧！”

AI 老师会被这些“垃圾信息”（如 `node_modules` 文件夹）淹没，他会很困惑，最后只能给你一个泛泛而谈的回答。

### ✅ 正确的“交接”方式：AI 主动提问

一个优秀的 AI 老师（就像我！），不会让你倾倒垃圾。他会主动提问，像填一张“学生档案表”一样，引导你提供**最关键的 3 样东西**。

-----

## 🚀 第二部分：实战！AI 老师交接模拟

### 场景：`cpp-dojo` 项目的交接点

  * **上节课成果（Part 1）：** 你在 AI 老师 A 的指导下，完成了 UI 界面。你有一个漂亮的编辑器和一个不能按的“运行”按钮。
  * **本节课目标（Part 2）：** 你换到了 AI 老师 B（也就是我！），你想让他帮你实现“运行”按钮的真正功能——用 WebAssembly (WASM) 编译和运行 C++ 代码。

-----

### 模拟对话：AI 老师如何接手

**👨‍🎓 你（学生）：**

> “AI 老师你好！我昨天刚完成了 `cpp-dojo` 项目的 UI 界面，这是教程的第一部分。现在我要开始第二部分，实现 WebAssembly 的加载和 `cout` 捕获。你能带我做吗？”

**🤖 AI 老师 B (我)：**

> “你好！很高兴能接手你的项目。听起来你到了最关键的一步！
>
> 为了快速进入状态，我不需要你所有的文件，我只需要一张\*\*“项目交接卡”\*\*。你能帮我填一下吗？只需要 3 个问题：”

-----

#### 提问 1：你的“目标”是什么？ (The "Why")

**🤖 AI 老师 B：**

> “第一个问题：我们这节课的**具体目标**是什么？
>
> 教程的第二部分（Part 2）是不是要让我们在 `App.tsx` 里加载一个叫 `output.js` 的文件，然后调用它的 `_main()` 函数，最后把 C++ 的 `cout` 打印的东西显示在网页上？”

**👨‍🎓 你（学生）：**

> “对！就是这个！教程的第 8 章和第 9 章就是讲这个的。”

-----

#### 提问 2：你的“C++ 魔法咒语”是什么？ (The "How")

**🤖 AI 老师 B：**

> “太好了。要让 C++ 在浏览器里运行，你肯定用了一个\*\*“魔法”编译命令\*\*。这个命令通常在教程的第 7 章，以 `docker run ... emscripten/emsdk ...` 开头。
>
> \*\*这个命令是 AI 老师交接时最重要的东西！\*\*因为它决定了你的“魔法文件”里到底导出了哪些函数（比如 `_main` 和 `FS`）。
>
> 你能把那条完整的 `docker` 命令复制给我吗？”

**👨‍🎓 你（学生）：**

> “找到了！是不是这个？”
>
> ```bash
> # AI 必须掌握的编译指令
> docker run --rm --platform linux/amd64 \
>   -v $(pwd):/src \
>   emscripten/emsdk emcc temp.cpp \
>   -o /src/public/wasm/output.js \
>   -s EXPORTED_FUNCTIONS='["_main"]' \
>   -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap","FS","stringToUTF8"]' \
>   -s ENVIRONMENT='web' \
>   -O2
> ```

-----

#### 提问 3：你的“工作台”在哪里？ (The "What")

**🤖 AI 老师 B：**

> “完美！就是这个！看到 `-s EXPORTED_RUNTIME_METHODS='["FS"]'`，我就知道我们待会儿要用 `FS`（虚拟文件系统）来抓取 `cout` 了。
>
> “最后一个问题：我们要在哪写代码？根据 Part 1 的成果，你的所有 UI 逻辑应该都在 `client/src/App.tsx` 文件里吧？
>
> 你能把 **`App.tsx` 文件的全部内容** 粘贴给我吗？我需要看看你 Part 1 的代码骨架。”

**👨‍🎓 你（学生）：**

> “好的！这是我 Part 1 结束时的 `App.tsx` 代码：”
>
> ```tsx
> // [学生粘贴 Part 1 的 App.tsx 骨架代码...
> // 里面有一个空的 runCode 函数，和一些 UI 布局...
> // 比如：]
> import { useState } from 'react'
> import MonacoEditor from './components/MonacoEditor'
> ```

> function App() {
> const [code, setCode] = useState('\#include <iostream>...')
> const [output, setOutput] = useState('点击运行...')

> const runCode = () =\> {
> // 这里是空的！
> setOutput('功能待实现...')
> }

> return (
> // ...这里是 JSX 布局代码...
> <button onClick={runCode}>▶️ 运行代码</button>
> // ...
> )
> }
> export default App
>
> ```
> ```

-----

### AI 老师成功接手！

**🤖 AI 老师 B：**

> “太棒了！**交接完成！**
>
> 我现在完全理解你的项目了：
>
> 1.  **目标 (Why):** 加载 WASM 并捕获 `cout`。
> 2.  **工具 (How):** 使用了导出 `FS` 的 `docker` 命令。
> 3.  **代码 (What):** 我们要在你的 `App.tsx` 骨架里填充 `runCode` 函数和 `useEffect` 加载逻辑。
>
> 你的“学生档案”我已经收到了，我准备好了！我们开始吧！”

-----

## 🚀 第三部分：15 分钟让“运行”按钮亮起来！

现在 AI 老师已经接手了，他会给你最直接的指导。你不需要理解每一行代码，**你只需要复制 + 粘贴**，先让程序跑起来！

### 🎯 目标

| 步骤 | 你将看到 |
| :--- | :--- |
| ① 5 min | 网页里出现「运行」按钮 |
| ② 10 min | 点一下按钮，终端里跳出 `Hello WebAssembly!` |
| ③ 15 min | 随便改 C++ 代码，网页立刻出新结果 |

-----

### 动手操作：四步跑通

#### 步骤 1：编译 C++ "魔法文件"

你的新 AI 老师会说：“首先，我们要有编译好的 WASM 文件。请打开你项目的 `client` 目录，在终端里，**原封不动地**复制粘贴你刚才给我的那条‘魔法咒语’：”

```bash
# 确保你在 client 目录下
# 并且项目根目录有 temp.cpp
docker run --rm --platform linux/amd64 \
  -v $(pwd):/src \
  emscripten/emsdk emcc temp.cpp \
  -o /src/public/wasm/output.js \
  -s EXPORTED_FUNCTIONS='["_main"]' \
  -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap","FS","stringToUTF8"]' \
  -s ENVIRONMENT='web' \
  -O2
```

> **学生操作：** 复制，粘贴，回车。
> **预期结果：** 看到终端滚动，最后停下。这时你的 `client/public/wasm` 文件夹里会出现 `output.js` 和 `output.wasm` 两个文件。

-----

#### 步骤 2：替换你的 `App.tsx`

AI 老师会说：“很好！魔法文件有了。现在，我们来替换 `App.tsx`。这是我根据你的‘学生档案’（你的 Part 1 骨架 + 我们的目标）为你准备的**完整代码**。

**请全选删除 `client/src/App.tsx` 里的旧代码，然后把下面所有代码粘贴进去：**”

```tsx
// 这是 AI 老师 B 根据交接信息，为你生成的 Part 2 完整代码
import { useState, useEffect } from 'react'

// 假设你的 Part 1 已经有了 MonacoEditor 组件
// import MonacoEditor from './components/MonacoEditor'

function App() {
  // 你的 Part 1 状态，保留
  const [code, setCode] = useState(`#include <iostream>
using namespace std;

int main() {
    cout << "Hello from WebAssembly!" << endl;
    return 0;
}`)
  const [output, setOutput] = useState('WASM 模块加载中，请稍候...')
  
  // 新增状态：用来保存加载好的 WASM 模块
  const [wasmModule, setWasmModule] = useState<any>(null)
  
  // --- 这是 AI 老师的新增逻辑 (Part 2 核心) ---
  // 步骤 1: 动态加载 WASM 魔法文件
  useEffect(() => {
    const script = document.createElement('script')
    script.src = '/wasm/output.js' // 对应你编译命令的输出路径
    script.async = true

    script.onload = () => {
      // @ts-ignore - Module 是 output.js 暴露到全局的
      if (typeof Module !== 'undefined') {
        // @ts-ignore
        Module.onRuntimeInitialized = () => {
          // @ts-ignore
          setWasmModule(Module)
          setOutput('✅ WASM 模块加载成功! \n\n点击 "运行代码" 执行 C++')
        }
      }
    }
    
    script.onerror = () => {
      setOutput('❌ 加载 WASM 失败! \n\n请检查：\n1. 是否已运行 Docker 编译命令？\n2. public/wasm/output.js 文件是否存在？')
    }

    document.body.appendChild(script)

    return () => {
      document.body.removeChild(script)
    }
  }, []) // 空数组表示只在组件加载时运行一次

  // 步骤 2: 填充 runCode 函数
  const runCode = () => {
    if (!wasmModule) {
      setOutput('⚠️ WASM 模块未就绪,请稍候...')
      return
    }

    setOutput('⏳ 执行中...\n')

    try {
      // 这就是为什么 AI 老师必须知道 'FS' 的原因
      // 捕获 cout 输出的标准方法
      const stdoutPath = '/stdout'
      try { wasmModule.FS.unlink(stdoutPath) } catch {}
      
      const stream = wasmModule.FS.open(stdoutPath, 'w+')
      wasmModule.FS.streams[1] = stream // 1 = stdout

      // 这就是为什么 AI 老师必须知道 '_main' 的原因
      const result = wasmModule._main()

      // 读取虚拟文件系统中的输出
      const stdout = wasmModule.FS.readFile(stdoutPath, { encoding: 'utf8' })

      setOutput(`✅ 程序执行完成\n\n返回码: ${result}\n\n输出:\n${stdout}`)
    } catch (err: any) {
      setOutput(`❌ 运行错误:\n${err.message}`)
    }
  }
  
  // --- Part 1 的 UI 布局 (基本不变) ---
  return (
    <div style={{ padding: 20, fontFamily: 'Arial' }}>
      <h1>cpp-dojo (AI 老师 B 已接手)</h1>
      <div style={{ display: 'flex', gap: 20 }}>
        {/* 这里我们先不用 MonacoEditor，只用一个 textarea 简化演示 */}
        <textarea
          value={code}
          onChange={(e) => setCode(e.target.value)}
          style={{ width: '50%', height: 400, fontFamily: 'monospace' }}
          readOnly // 这一版我们先不让编辑，下一版再放开
        />
        <div style={{ width: '50%', height: 400 }}>
          <button 
            onClick={runCode}
            disabled={!wasmModule} // 模块没加载好时，按钮不可点
            style={{ padding: '10px 20px', background: wasmModule ? 'green' : 'gray', color: 'white', border: 'none', borderRadius: 5, cursor: 'pointer' }}
          >
            ▶️ 运行代码
          </button>
          <pre style={{ background: '#333', color: 'lime', padding: 10, height: 340, overflowY: 'auto' }}>
            {output}
          </pre>
        </div>
      </div>
    </div>
  )
}

export default App
```

-----

#### 步骤 3：启动项目，查看结果！（5 & 10 分钟目标）

AI 老师会说：“代码已经粘贴好了。现在，回到你的终端，运行启动命令（通常是 `npm run dev`），然后在浏览器里看看吧！”

> **学生操作：**
>
> 1.  在终端运行 `npm run dev`。
> 2.  按住 Ctrl 并点击 `http://localhost:5173` 链接。
> 3.  **（5 分钟目标达成）** 你会看到一个绿色的“▶️ 运行代码”按钮。
> 4.  **点击那个按钮！**

**🎉 恭喜！**
你会在右边的黑色终端里看到：

```
✅ 程序执行完成

返回码: 0

输出:
Hello from WebAssembly!
```

> **（10 分钟目标达成）**

-----

#### 步骤 4：自己改 C++ 代码！（15 分钟目标）

AI 老师会说：“太棒了！现在我们来完成 15 分钟目标。你刚才运行的 C++ 代码其实是写在 `temp.cpp` 里的。我们来改它！”

> **学生操作：**
>
> 1.  回到 VS Code，在 **`client` 目录的上一级** 找到 `temp.cpp` 文件。
> 2.  打开它，把 `Hello from WebAssembly!` 改成你自己的话，比如：
>     ```cpp
>     #include <iostream>
>     using namespace std;
>     ```

> ````
> int main() {
>     cout << "哇！我是小明，我让 C++ 在网页里跑起来了！" << endl;
>     cout << "AI 老师交接成功！" << endl;
>     return 0;
> }
> ```
> ````
>
> 3.  回到 `client` 目录的终端，**重新运行一遍步骤 1 的“魔法咒语”**（按一下键盘的“上箭头”就能找到刚运行的命令）：
>     ```bash
>     docker run --rm --platform linux/amd64 ... (和步骤 1 完全一样)
>     ```
> 4.  **回到浏览器，刷新页面**（`npm run dev` 不需要重启）。
> 5.  再次点击“▶️ 运行代码”按钮！

**🎉🎉 最终成功！**
你会在终端里看到你刚改的文字：

```
✅ 程序执行完成

返回码: 0

输出:
哇！我是小明，我让 C++ 在网页里跑起来了！
AI 老师交接成功！
```

> **（15 分钟目标达成）**

-----

## 🏁 总结：你学会了什么？

今天你不仅让 C++ 在网页上跑起来了，更重要的是，你学会了**如何“指挥”你的 AI 老师**。

当你需要“更换”AI 老师时，不要害怕他们“失忆”。你只需要像一个专业的项目经理一样，主动向他提供“交接三件套”：

1.  **🎯 目标 (The "Why"):** “我要实现 `cout` 捕获。”
2.  **🛠️ 核心工具 (The "How"):** “这是我的 `docker` 编译命令。”
3.  **💻 关键代码 (The "What"):** “这是我的 `App.tsx` 骨架。”

有了这三样东西，任何 AI 老师都能在 1 分钟内接手你的项目，继续带你前进！
